#features of encapsulation:

#In the case of encapsulation private members are only accessible inside the class which defines them.
#python achieves encapsulation through a process called as name mangling where the property is internally renamed as '_classname__propertyname'.